"""ShapePipe test suite.

One discovery root. ``tests`` is a package so the suite-level helpers import
as ``tests.helpers.*`` (e.g. the star-response R-function, artifact emission)
regardless of which tier a test sits in. See ``tests/README.md`` for the layout.
"""
