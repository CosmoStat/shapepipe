"""Module-unit tests.

Per-module tests that import package internals directly — the fitter, file
handler, split-exp, vignetmaker, ngmix internals, the GalSim weight-validation
suite. Relocated here from ``src/shapepipe/tests/`` so the whole suite lives
under one discovery root.
"""
